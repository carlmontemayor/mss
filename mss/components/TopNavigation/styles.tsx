// Imports
import { styled } from '@material-ui/core/styles';
import { AppBar } from '@material-ui/core';

// Styled AppBar from Material UI
export const TopNavigation = styled(AppBar)({
  color: 'black',
  backgroundColor: 'papayawhip',
});
